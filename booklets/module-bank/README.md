# Teaching module bank

Approved reusable teaching modules are stored here as `mathsmap-teaching-module-v1` JSON. Draft AGY output never belongs in this directory; modules arrive only through an explicitly approved full-booklet publication.
