# Archived Linear Relationships pilots

Archived on 6 September 2026 at the user's request. The three saved pilot projects and their available revision histories were copied, verified against SHA-256 hashes, and removed from the active Studio project directory.

| Project | Last saved revision |
|---|---:|
| `linear-relationships-pilot-v1` | 18 |
| `project-linear-relationships-assembled-pilot-v1` | 5 |
| `project-linear-relationships-studio-sample-v1` | 9 |

`manifest.json` inventories all 32 archived files with original locations, sizes and hashes. Assets, source evidence, benchmark records and historical exports remain at their original paths so the archived projects retain their references.

The final three archived project snapshots and this manifest are versioned in Git. Automatic `.revisions` histories remain local and are ignored; a fresh clone does not contain every file in the historical manifest. Restore the final snapshot from Git, or use the retained local archive for its earlier revisions.

The active successor is `booklets/projects/linear-relationships-complete-v1.json`. It carries forward the latest corrected pilot content within the complete 93-page booklet. The archive itself is unchanged by subsequent booklet repairs.

To restore a pilot, first check that its original paths are unoccupied, verify its archive hashes, then copy its project and revision files back to the manifest's `original` locations. Restoration should not overwrite an existing project.
