# Linear Relationships predecessors

The canonical booklet is `booklets/projects/linear-relationships-v1.json` (Linear Relationships v1), copied without content or layout changes from Compact Exercise Trial revision 8 on 8 September 2026.

Archived projects:

- Linear Relationships v0: formerly Linear Relationships, revision 165. Its stable project ID and filename are retained; only the displayed title changed.
- Linear Relationships — Flexible, revision 6.
- Linear Relationships — Compact Exercise Trial, revision 8.

`manifest.json` records paths and hashes. The 168 bank sync owners transferred to v1 with their baselines unchanged; bank content was unchanged. `bank-sync-links-before.json` is the pre-transfer recovery record, not the active registry.

Existing automatic revision histories are preserved under `projects/.revisions` locally and remain ignored by Git. The unrenamed v0 snapshot is also preserved there. Shared assets, source documents and render evidence remain at their existing paths. To restore a project, copy its archived JSON to its unoccupied original path; restoring bank ownership requires explicit reconciliation with v1.
