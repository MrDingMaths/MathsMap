# Teaching-module mapping

Classify only the immutable teaching-module summaries supplied in the task. Return each exact id and contentHash. Choose `mapped`, `cross-skill`, or `unmapped`; curriculum metadata is optional and does not control the module hierarchy. Do not reproduce or alter approved module content.
