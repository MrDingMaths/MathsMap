# Dedicated TikZ reconstruction stage v4

House style overrides source origin lettering: do not print O at a Cartesian grid origin. Preserve named mathematical points elsewhere.

Return only JSON {"diagrams":[{"id":"existing diagram id","code":"complete tikzpicture","uncertainties":[]}]} for every requested diagram exactly once. Do not change any other content or IDs. Use the supplied source images and this arm's reconstruction specification. No access to other results. Recreate source visual meaning, scale, labels, dots, colours, scaffolds and axis bounds. Plot equations and coordinates mathematically, not traced guesses. Draw actual solution sketches where requested; describing a graph is insufficient. Use a clipping scope around mathematical plot content so lines cannot continue past the plot area; leave axis labels outside that clipping scope. Same base geometry and coordinate frame for solutions and question figures. Follow the full supplied TikZ manual. Any unresolved contradiction must appear in uncertainties; do not fabricate geometry.


For role:solution, draw a complete single answer figure including its grid/axes as needed. For role:solution-overlay, draw ONLY the additional marks; never duplicate the base axes, grid or source labels. Match the base options and explicit bounding box exactly. Shared solution figures must plot every requested coordinate, not only the first leaf. Source visible point labels and scaffolds remain on question figures; independently derived answer coordinates must not leak into them. Verify all line endpoints against the stated axis bounds before returning code.


Editable mathematics is mandatory: Cartesian function graphs must use PGFPlots `axis` and equation-valued `addplot` expressions with explicit axis bounds, tick intervals and clipping. Do not flatten a known equation into endpoint segments or trace a line from image pixels. A fixed coordinate list is appropriate for discrete supplied points, not as a replacement for a known function. Retain mathematicalModel with bounds, lines [{m,c,colour}] (or {x,colour} for a vertical line), points, and visible labels in each graph result. Blank grids must also be native mathematical axes; do not retain ordinary graph images. Keep raster images only for non-graph illustrations, software UI screenshots, or explicitly unresolved evidence. Student figures must preserve only source-given data; inferred equations belong in the model/code, not automatically in visible labels.

Graph typography at final printed size: 8.5 pt axis numbers; 10 pt axis, coordinate and equation labels. Allow 8 pt numbers only for crowding, with a final-size visual collision check. Preserve source presence or absence of numerical scales and tick marks; never add them automatically. For unscaled axes retain source grid lines, axes, arrows and x/y labels, with tickLabels:false and ticks:false in the graph model. Do not apply blanket node enlargement.

Graph strokes at final printed size: plotted relationships 0.8 pt (including dashed relationships), axes 0.5 pt, ticks 0.4 pt, major grids 0.25 pt, minor grids 0.15 pt, construction guides 0.4 pt. Use explicit line widths by role, never thin/thick keywords. Include \special{dvisvgm:raw <metadata data-graph-strokes="1"/>} inside the tikzpicture so preview and PDF compensate stroke widths for SVG fitting. Preserve colours, dashes, arrowheads and labels.
