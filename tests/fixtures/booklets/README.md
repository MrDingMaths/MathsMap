# Booklet compatibility fixtures

Frozen historical inputs used only by regression checks. `linear-legacy-layout.json` retains the full source-page structure to test conversion, all boundaries and continuations. `linear-editor-layout.json` supports isolated legacy editor checks. `linear-flexible-settings.json` retains one section to verify that loading an existing flexible project does not enable compact defaults. These are not Studio projects or import candidates.
