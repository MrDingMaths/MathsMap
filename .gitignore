node_modules
dist
.DS_Store
*.local
.env
.env.*
.claude/

# Extracted booklet images (regenerate via pandoc --extract-media; not needed in git)
booklets/**/media/
.checkwork/

# agy transcription/generation scratch
.agywork/
.booklet-work/
# Includes local recovery copies and cleanup manifests; never release them.


# Generation-agent bake-off scratch (opus vs codex pilot)
.pilot/

# Visual-gate scratch
.shots*/
scratch_*.png
public/.audit-input/

# Diagnostic Questions browser profile, screenshots, and resumable work queue
.diagnostic-questions/

# Session scratch (never commit)
scratch/
task-*.result.json
.tmp-*.mjs
tmp/
# Local browser and booklet-render scratch
.playwright-mcp/
.tmp-*/
output/

# Automatic project save history stays local; current projects are versioned by Git.
booklets/projects/.revisions/
booklets/archives/**/projects/.revisions/

# Verified, indexed local recovery archives (never release payloads or indexes).
booklets/.storage-archives/
# Legacy local recovery material; current booklets live in booklets/projects.
booklets/archives/
